version: "3.6"

services:
  postgres:
    build: "./postgres"
    restart: "always"
    environment:
      POSTGRES_DB: "orderfood"
      POSTGRES_USER: "orderfood"
      POSTGRES_PASSWORD: "orderfood"